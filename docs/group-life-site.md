# Group Life member website

The Group Life directory excludes closed groups and groups where the viewer's membership status is `removed`, even for administrators. Other discoverable groups remain available to join. Sidebar entries require an approved membership and an open group in the viewer-scoped directory. Both surfaces share the directory query; closing/updating groups or changing memberships invalidates it. This is a presentation rule, not a change to management access or API authorization. 简体：小组生活目录和导航不显示已关闭的小组，目录也排除本人已退出的小组，其他可发现的小组仍可申请加入。繁體：小組生活目錄與導覽不顯示已關閉的小組，目錄也排除本人已退出的小組，其他可發現的小組仍可申請加入。

Group leaders can permanently dissolve an empty non-church group from Profile & settings after server eligibility checks and explicit confirmation. See [Group dissolution](group-dissolution.md) for the API, data safeguards, bilingual behavior and verification. 简体：组长可在资料与设置中，经服务器校验和明确确认后解散空小组。繁體：組長可在資料與設定中，經伺服器校驗和明確確認後解散空小組。

Group Life combines Home, Announcements, Albums, Forum, and Events in that order beneath a persistent dark title bar, using the same navigation component as [Church Life](church-life-site.md). The forum tab is labelled **Forum / 论坛**. The title bar remains outside animated route content, so switching these sections does not remount it. Section descriptions appear beneath the group name and above the menu. Tabs scroll on narrow screens and support keyboard focus and activation.

The primary navigation label is always **Group Life / 小组生活**. Its second-level entries list approved memberships in open, visible non-church groups, ordered by group leader, co-leader, then active member, preserving membership order within each role. Active means approved membership, not an activity score. Each entry opens an explicit group URL without selecting a global current group. The primary destination is `/groups`, a directory of open groups discoverable by the viewer, including groups not yet joined but excluding groups the viewer has left. The directory has two tabs: Fellowships / 团契 and Ministries / 事工组, using the stored groupType with missing values treated as fellowship. It shows summary rows, not cards: name, membership state, and a short description. A row expands to show a small display picture, full description, content access where permitted, and a separate join/request-status action. Only the expanded row loads its first accessible page image using the former group-selection image fields; missing or failed images use the existing site fallback. Images have alt text and fixed dimensions. Expansion is saved in the URL and preserved on return. Search, membership filters, name sorting, and pagination (12 per page) are stored in the URL. The organization-tree entry and page are removed; old `/groups/select/tree` bookmarks redirect to `/groups`. The root church stays under Church Life. Mobile primary navigation opens the directory first when entering Group Life. Group Management is a title-bar action within each group, not a second-level navigation entry; existing manager permissions remain intact. Announcements, albums, forum, and events appear in the website menu instead of separate sidebar entries. Group Management no longer shows a pending-review badge; membership review itself is unchanged.

Group Management calls its child-group section **Ministries / 事工**, and creates only ministry groups; the backend enforces this rule. Historical child fellowships remain accessible and labelled. Its duplicate Albums entry/panel is removed; Group Life retains album functionality. Legacy management `section=albums` links return to profile. See [Group types](group-types.md) for persistence, creation compatibility, and migration requirements.

简体：小组管理的“下属小组”改为“事工”，仅可新增事工组，由后端强制校验；历史下属团契保留并标明类型。移除管理页重复的相册入口及面板，小组生活相册功能保留；旧管理 `section=albums` 链接回到资料页。类型、创建兼容性和迁移要求见 [Group types](group-types.md)。

繁體：小組管理的「下屬小組」改為「事工」，僅可新增事工組，由後端強制校驗；歷史下屬團契保留並標明類型。移除管理頁重複的相冊入口及面板，小組生活相冊功能保留；舊管理 `section=albums` 連結回到資料頁。類型、建立相容性和遷移要求見 [Group types](group-types.md)。

**Group Management** remains a title-bar action for existing managers of the displayed group. Non-members can open the join/status page from the title bar. A **Back to Group Life** link returns to the directory and its filters. There is no Switch group action or current-group badge. Group pages, menus, and join completion use explicit group URLs. On narrow screens they use labelled icon buttons with accessible names.

The group management Members view contains three independent collapsible lists: Pending, Active members, and Inactive members. Each summary always shows its count. Lists start collapsed and can be opened with a pointer, Enter, or Space; empty categories show an explanatory message. Expanding or collapsing a list does not reload memberships or change permissions. Approval, role, profile, and removal controls remain inside their existing member rows. Expansion survives language-only changes while the view remains mounted.

Existing access rules remain intact: group managers see announcements and events management; approved group members and administrators see albums and forum. The home tab remains the member-facing overview. The menu uses existing route and API guards and does not grant additional permissions. New navigation uses `/groups/:groupId` URLs. `/groups` and `/groups?view=overview` show the directory; `/groups/select` redirects there. Legacy `/groups?section=...` or `?page=...` bookmarks resolve the saved group into an explicit URL for compatibility. Other legacy scoped routes retain their compatibility support; browsing the directory does not select or change that saved value. Management and event details stay separate working areas. Album and forum post details retain the site header and their own detail title/back link. Forum create/refresh actions remain with the category filter, and existing group-scoped requests, category filtering, and pagination remain intact.

No API, authorization, persistence, or cache contract changes are required for this navigation change. Header group data is keyed by viewer and group, not language. Verify all five sections as a manager, member album/forum access, multi-group ordering and management placement, explicit-group links, forum post detail/back navigation, keyboard navigation, and title-bar DOM identity at mobile/desktop widths in English and Chinese.

Group profile settings omit general handover instructions, missing-leader notices, and unavailable handover controls. The current approved leader still sees actions for eligible approved co-leaders, with existing confirmation and authorization. Group directory rows show the viewer's approved role (Group leader, Co-leader, or Group member) instead of Joined. Non-members see Available to join; pending, invited, and guest states remain explicit. Content and application actions appear only in the expanded details. Expanding a summary does not select a global current group. Tabs support arrow-key focus and Enter activation, with one tab panel and a non-wrapping mobile tab row.

Group Life and Group Management retain the greatest measured body height within the current viewer/group workspace and width, so lazy loading or shorter tabs do not collapse the document or move its scroll position. Shorter tabs may leave space below the content. Changing workspace identity or width resets the measurement; internal pages reserve scrollbar space. Verify long-to-short tab transitions during loading, at desktop and mobile widths, alongside the role labels and handover visibility.

Also verify header action placement, manager/member visibility, the explicit-group management target, all three category counts, empty categories, independent expansion, keyboard operation, and that toggling categories does not trigger membership requests.

简体：小组生活顶部菜单依次为首页、公告、相册、论坛、活动，其中论坛标签为“论坛”。切换菜单只更新内容区，标题栏不重新挂载；说明位于组名下、菜单上。一级栏目固定为“小组生活”，二级列出全部已批准加入的非教会小组，按组长、副组长、活跃组员排序，同角色保持原顺序。活跃指已批准的成员资格，不按活跃度评分。一级入口打开小组概要目录，展示当前用户可发现的所有小组（含尚未加入的小组），支持搜索、加入状态筛选、名称排序和每页十二组的分页；条件保存在网址中。目录设“团契”和“事工组”两个标签，按已存类型分类，缺少类型按团契处理。采用概要行与展开详情，概要显示名称、身份和简短介绍；展开后显示小幅展示图片、完整介绍及内容/申请操作。图片按需读取首个可访问页面的原有图片字段，缺失或失败使用现有默认图片；有替代文字和固定尺寸。展开状态保存在网址中。移除组织树入口及页面，旧链接返回目录。二级入口使用指定小组网址，不设置全局当前小组。小组管理位于各小组标题栏，不再作为二级栏目，原有管理和审核权限不变。公告及活动管理仍限原有管理权限，相册和论坛仍限已批准成员或管理员。当前小组与指定小组的导航保持各自的小组范围；管理和活动详情仍为独立工作区，相册及帖子详情保留网站标题栏、详情标题与返回链接。论坛发帖及刷新操作位于分类筛选旁，原有小组范围、分类及分页不变。手机菜单单行滚动，支持键盘及中英文切换，不因语言变化重新获取数据。

移除“切换小组”和当前选中标记；标题栏提供返回小组生活（保留目录筛选）、小组管理及非成员的加入/状态入口，申请完成后进入指定小组网址；管理按钮仅对原有管理权限开放，并保留指定小组范围，手机使用带无障碍名称的图标按钮。成员页按待审批、活跃成员、非活跃三类显示人数和独立的伸缩列表，默认收起，支持点击及 Enter、空格键。空分类有提示，展开收起不重新读取成员；原有审批、角色、资料与移除权限不变。页面保持挂载时，切换语言保留展开状态。

资料页移除移交职责的泛化说明、未找到组长的提示及不可用的移交操作；只有当前已批准的组长看到移交给合格副组长的操作，保留确认与原有授权。小组目录概要行按实际身份显示组长、副组长、组员或可以申请加入，保留申请中、受邀和访客状态。内容及申请操作仅位于展开详情内，展开不会选中全局当前小组。类型标签支持方向键聚焦与 Enter 激活，手机单行显示。小组生活与小组管理在同一用户和小组、同一宽度下保留内容区已测得的最大高度，避免加载和长短页面切换造成滚动跳动；短页面下方可能留白，切换工作区身份或宽度后重新测量，内部页面预留滚动条空间。

繁體：小組生活頂部選單依次為首頁、公告、相冊、論壇、活動，其中論壇標籤為「論壇」。切換選單只更新內容區，標題欄不重新掛載；說明位於組名下、選單上。一級欄目固定為「小組生活」，二級列出全部已批准加入的非教會小組，按組長、副組長、活躍組員排序，同角色保持原順序。活躍指已批准的成員資格，不按活躍度評分。一級入口開啟小組概要目錄，展示目前使用者可發現的所有小組（含尚未加入的小組），支援搜尋、加入狀態篩選、名稱排序和每頁十二組的分頁；條件儲存在網址中。目錄設「團契」和「事工組」兩個標籤，按已存類型分類，缺少類型按團契處理。採用概要列與展開詳情，概要顯示名稱、身分和簡短介紹；展開後顯示小幅展示圖片、完整介紹及內容/申請操作。圖片按需讀取首個可存取頁面的原有圖片欄位，缺失或失敗使用現有預設圖片；有替代文字和固定尺寸。展開狀態儲存在網址中。移除組織樹入口及頁面，舊連結返回目錄。二級入口使用指定小組網址，不設定全域目前小組。小組管理位於各小組標題欄，不再作為二級欄目，原有管理和審核權限不變。公告及活動管理仍限原有管理權限，相冊和論壇仍限已批准成員或管理員。目前小組與指定小組的導覽保持各自的小組範圍；管理和活動詳情仍為獨立工作區，相冊及帖子詳情保留網站標題欄、詳情標題與返回連結。論壇發帖及重新整理操作位於分類篩選旁，原有小組範圍、分類及分頁不變。手機選單單行捲動，支援鍵盤及中英文切換，不因語言變化重新取得資料。

移除「切換小組」和目前選中標記；標題欄提供返回小組生活（保留目錄篩選）、小組管理及非成員的加入/狀態入口，申請完成後進入指定小組網址；管理按鈕僅對原有管理權限開放，並保留指定小組範圍，手機使用帶無障礙名稱的圖示按鈕。成員頁按待審批、活躍成員、非活躍三類顯示人數和獨立的伸縮列表，預設收起，支援點擊及 Enter、空白鍵。空分類有提示，展開收起不重新讀取成員；原有審批、角色、資料與移除權限不變。頁面保持掛載時，切換語言保留展開狀態。

資料頁移除移交職責的泛化說明、未找到組長的提示及不可用的移交操作；只有目前已批准的組長看到移交給合格副組長的操作，保留確認與原有授權。小組目錄概要列按實際身分顯示組長、副組長、組員或可以申請加入，保留申請中、受邀和訪客狀態。內容及申請操作僅位於展開詳情內，展開不會選中全域目前小組。類型標籤支援方向鍵聚焦與 Enter 啟用，手機單行顯示。小組生活與小組管理在同一使用者和小組、同一寬度下保留內容區已測得的最大高度，避免載入和長短頁面切換造成捲動跳動；短頁面下方可能留白，切換工作區身分或寬度後重新測量，內部頁面預留捲軸空間。

简体：`/groups` 和旧总览入口显示目录，`/groups/select` 跳转至目录；旧 `section` 或 `page` 书签可将保存的小组转换为指定网址以保持兼容，其他旧范围路由仍保留兼容支持。浏览目录不会改变保存值。

繁體：`/groups` 和舊總覽入口顯示目錄，`/groups/select` 跳轉至目錄；舊 `section` 或 `page` 書籤可將儲存的小組轉換為指定網址以保持相容，其他舊範圍路由仍保留相容支援。瀏覽目錄不會改變儲存值。
