# Cloudflare Worker local secrets for `wrangler dev` / `npm run preview`.
# Copy to .dev.vars (gitignored) and fill in real values.
GEMINI_API_KEY=your-gemini-api-key
CACHE_SYNC_API_TOKEN=your-high-entropy-cache-sync-token
