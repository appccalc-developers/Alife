// Keep the site-wide forum routes available for backward-compatible direct links,
// while exposing forum navigation only inside a selected group workspace.
export const siteForumEntryEnabled = false
